import turtle

window = turtle.getscreen()
t = turtle.Turtle()

window.title("ფიგურების ხატვა")
# window.setup(1000, 600, 0, 0)
# window.setup(width=1000, height=600, startx=0, starty=0)
# window.setup(height=600, starty=0)
window.setup(1000, 600)




# =======================
turtle.exitonclick()