import turtle

s = turtle.getscreen()
t = turtle.Turtle()

t.speed(1)

##r = 100
##t.circle(-r, 90)

'''
forward   => fd
backward  => bk,  back
right     => rt
left      => lt
'''

t.goto(-75, 0)

t.goto(75, 0)
t.goto(0, 200)
t.goto(-70, 0)
t.home()
t.goto(0, 200)









# =================
turtle.done()
